import React from 'react';
import ReactDOM from 'react-dom/client';
import AgentePlanning from './AgentePlanning';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <AgentePlanning />
  </React.StrictMode>
);
